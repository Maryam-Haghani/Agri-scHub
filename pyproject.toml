[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Agri-scHub"
authors = [{name = "SongLiLab", email = "song@vt"}]
dynamic = ["version", "description"]
dependencies = [
    "sphinx"
]
